{
  "name": "googledoc-grid-drawer",
  "version": "1.0.0",
  "description": "script for fetching the content of a google doc and generating a grid from the characters in the table",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "node-fetch": "3.3.2"
  },
  "author": "",
  "license": "ISC"
}
